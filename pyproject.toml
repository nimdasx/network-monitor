[project]
name = "network-monitor"
version = "0.1.0"
description = "Realtime network monitoring TUI"
requires-python = ">=3.12"
dependencies = [
    "ping3>=5.1.5",
    "rich>=15.0.0",
    "textual>=8.2.6",
]
